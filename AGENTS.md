# 山兔项目协作与同步

## 项目目标

手机优先、面向全球的三维地图/天气/户外轨迹工作台，产品名“山兔”（原名观云）。界面与默认视角不限定成都或川西；地区数据的真实覆盖和署名必须保留，不把局部增强数据说成全球覆盖。包名、签名、存储键和备份格式保持兼容。优先模块化、低耦合、可扩展和可维护；修复只改必要范围，不顺手重构无关模块。跨模块通过类型接口、props、事件和适配器通信。

开始工作先读 `CURRENT_STATE.md`，再读 `docs/agent-handoff.md` 和相关模块与文档。2026-09-21用户已明确回退到0.2.35，当前分支为 `codex/rollback-ui-0235-20260921`；旧UI工作树仅作追溯，不再继续PDF还原。修改前说明涉及模块/文件及不受影响的范围。长任务持续维护进度；重要修改后记录已改文件、验证结果、限制和下一步。大量输出写本地 `.openai/*.log`，不要提交运行日志。

## GitHub 同步（用户已授权的长期偏好）

唯一项目同步目标：**https://github.com/Siger1989/map**，Git 远程名 `origin`，主分支 `main`。

- 用户在 2026-09-05 明确要求：后续项目内容同步到此仓库，以便换电脑继续研究。每轮完成用户授权的项目修改后，更新 `CURRENT_STATE.md` 和必要说明，执行相应检查，提交并推送当前工作成果；用户明确要求暂不推送时遵从新指示。
- 开始跨设备工作先检查工作区与远程更新；工作区干净时使用 `git pull --ff-only`。有本地未提交改动或分叉时先保留并整合，不能覆盖。不要强制推送，不重写他人历史。若使用 `codex/` 功能分支，说明实际推送分支，不声称已合入主分支。
- 完成后核对 GitHub 远程提交 SHA 与本地一致，再向用户报告同步成功；网络或认证失败要明确说明本地提交和远程状态，不假装已上传。
- 代码、测试、构建脚本、文档和已授权的必要地形资源进 Git；APK 与校验文件放 GitHub 的测试版 Release，不反复将大安装包提交到源码历史。只有生成新包时才发布对应版本，不能用旧包冒充新代码构建。
- `.env.local`、服务 Token、私钥、APK 签名密钥、本机缓存/日志和依赖目录不上传。需要地图服务授权时使用 `config/geocloud.env.example` 空模板，不能把真实凭证填进仓库。
- 不自动发布到 Sites 或改仓库可见性；用户此前希望保留不需要 OpenAI 登录的本地预览。

## 后续安装包目标（用户2026-09-08要求）

- 用户明确要求“每次改动就更新APK”：每轮完成项目改动后，默认提升安装包版本号/versionCode，使用最终源码重新构建、核对签名与资源，并与源码一同发布到GitHub测试Release，直接提供新APK下载链接，无需用户再次提醒。构建或发布受阻时记录具体原因，不能用旧包代替。本规则自当前轮起执行，用户明确暂不出包时遵从新指示。
- 后续用户要求生成APK时，同时安排鸿蒙设备可用的安装交付，并在同一发行说明列出各平台产物、支持系统、安装方式与验证状态。
- 用户已确认目标是朋友的 HarmonyOS 6.1 手机，明确要求原生包并希望直接分享安装；具体机型未知，不以 USB 连接本机为前提。原生版可考虑邀请测试链接分发；卓易通仅为备选兼容试装。用户提到 uni-app/uvue，可评估独立容器复用现有 React 地图，仍需鸿蒙工具链、能力适配和合法签名/分发；当前华为下载要求登录，用户暂时无法登录且没有或不确定开发者账号。见 docs/harmonyos-6.1-install.md。
- 目前仓库只有Android原生外壳，无HarmonyOS原生构建工程。首次鸿蒙出包前先完成平台适配与安装验证；尚未生成或无法安装时明确列为未交付，不能宣称已支持。
- 原生适配优先复用地图网页及业务模块，定位/后台轨迹、文件与照片、权限、分享、返回和本地资源由平台适配层承接。保留既有Android签名和数据格式。

## 手机界面约束（用户 2026-09-06 要求）

- 应用 Logo（用户 2026-09-09 指定）：统一使用 `public/brand/shantu-logo.png` 的深绿底、浅色兔子与山路原图。后续应用内品牌和各平台桌面图标沿用此图；适配尺寸保持图形比例，不另画替代 Logo。

- 后续 UI 按用户 582×1280 竖屏截图的比例优先设计；浏览器以 390×844 CSS 像素为基准，并检查 360×780 窄屏，不能把截图物理像素当作 CSS 宽度。
- 地图是主要操作区。面板只显示当前步骤；新建、列表、参数编辑分开，说明按需展开。压缩空白和重复内容，不以缩小触控目标换空间。
- 标记编辑浮窗不超过 38dvh / 320px，避开右侧地图工具和底部导航；内容内部滚动，关闭、返回和参数分组入口保持可见。放置或地图拖动时收起参数面板。
- 每次涉及 UI 的变更检查上述手机尺寸下的遮挡、横向溢出和关键操作可达性；截图保存 artifacts/screenshots/，记录结果。浏览器模拟不能代替真机触控验收。

## UI密度硬上限（用户2026-09-09晚间新增，覆盖上文旧浮窗尺寸）

所有地图操作浮窗复用 `docs/ui-density-standard.md`：普通操作浮窗最高200px并扣除当前可视区域安全占位，宽度最多300px，间距不超过4px。2026-09-10用户修正：内部字号/间距参考收藏36px行，地图直接操作保持44px；键盘弹出不按28dvh再次缩框。收藏目录约72%且下方地图至少180px；测量投影图框默认显示在控制滚动区之外；二者为明确内容例外，不能硬套200px裁掉内容。新增或修改浮窗均需检查实际CSS尺寸。

## 验证与交接

### UI快速确认流程（用户2026-09-21最新要求）

- 节约额度，只做用户当前指定的问题；按“最小修改 → 必要定向检查 → 实际截图 → 用户确认 → 下一步”推进。
- 视觉讨论中不要穿插不紧急的重构、整理、全套测试、打包或发布。上文每轮出包/同步规则在这种快速视觉确认阶段延后到明确交付时执行，除非用户当轮直接要求。
- 主页规格和本轮故障复盘以 `docs/homepage-ui-spec.md` 为准。不要只看CSS声明：核对实际边界、min/max-height、box-sizing与旧样式覆盖；背景不可盖住控件，确认视觉和实际点击都正常。
- 右侧预览按用户明确要求保持390×844；不得测试完恢复桌面尺寸。涉及响应式变化时再做相关尺寸检查，不为一个局部文字/间距修改重复全矩阵。
- 未经用户确认，不把“构建通过”“无溢出”写成PDF视觉定稿；不要自动推进其他页面。

按修改范围执行类型检查、相关逻辑检查与构建。既有命令：

```sh
npx tsc --noEmit
node --experimental-strip-types --test tests/*.test.mjs
npm run build
```

安卓需要 Windows、JDK17、Android SDK35/build-tools35.0.0；见 `docs/continue-development.md` 和 `mobile/README.md`。未经真机验证不得声称触控手感/性能已验收。旧数据源仍含非商业许可，地质云1:20万仍需本应用有效授权；不得把原型说成可直接销售的正式版本。

交付总结应包含修改模块/文件、新增/删除、未受影响模块、验证与限制、GitHub 提交或 Release 链接。开发机器路径不固定，使用项目相对路径和可配置 SDK/JDK 参数。
