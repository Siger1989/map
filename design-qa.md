# 拉力路书首版视觉复核 — 2026-09-21

- 目标：用户选定的853×1844生成效果图；实际390×844 CSS手机页，523×696外框截图中手机缩放为314×680。双方归一到390×844同图检查：`artifacts/screenshots/rally-20260921/comparison.png`，实际独图 `phone-390.png`。
- 状态差异：参考为演示中的骑行/定位/急弯；实际为真实驾车路线预览、定位不可用。没有移植示例速度、弯道或当前位置。当前测试路线仅出发、道路出口/直连接入信息。
- 首轮发现P2：后续指令字号/图标偏小，泛用路线图标容易误认转弯，未隐藏旧图层/天气入口。已加粗放大并改接入/未知语义，隐藏重复入口，重新截图对比。
- 本轮布局通过：独立上部路书/指标和下部地图/剖面，无叠窗与横向溢出；实际地图比参考高，以确保地图可见。字体沿用项目中文系统字体；同绿色导航与白色面板；真实地图/高程无生成背景，正文保留真实路线状态。关键两行提示已单独结合用户局部截图检查。
- 明确差异：不实现参考未确认的语音按钮，用说明/退出替代；累计升降为已行部分，无定位未知；底栏无额外选中状态。不是像素级复制，也不是赛事标准模板。
- 最终结果：本次390手机布局复核通过，等待用户视觉确认。360窄屏、真机GPS与真实转弯序列验收未完成。以下保留主页历史检查。

# 主页第二轮视觉检查 — 2026-09-21

范围：PDF第3–7页的主页布局；未展开整套内部页面改版。0.2.40首轮已被用户否定，以下为0.2.41第二轮，最终视觉仍由用户对照确认。

## 参考与实际

- 参考：`docs/reference/ui-discussion-20260916.pdf` 第3页853×1844主图，等比归一至390×844。对照：`artifacts/screenshots/home-responsive-20260921/comparison.png`。
- 实际预览：`http://127.0.0.1:9423`，右侧固定390×844。人工GPX“山脊环线 · 布局示例”只通过预览导入，不进入源码包或APK，不作为导航数据。
- 地图使用真实MapLibre底图；天气、GPS和耗时显示真实状态。参考中的地形插图、红色工具圈、录制状态及示意数值不伪装成产品数据。

| 控件 | PDF等比参考 | 第二轮390视口实际 | 结论 |
| --- | --- | --- | --- |
| 路线卡 | 约272×92 | 273×100，y58 | 恢复摘要三行；原首轮300×160.5明显过大 |
| 行程点 | 约94宽 | 101.4×100，y166 | 宽度贴近；当前仅起终点，参考还有观察点 |
| 记录窗 | 约232×86 | 234×98，y652.9 | 状态/数据/操作三行；当前未开始，参考记录中 |
| 右侧工具 | 约40宽 | 44×44 | 为直接地图触控保留44目标 |
| 底栏 | 约57高 | 56高，五项等宽 | 宽度自适应；选中浅绿 |
| 顶部四白框 | 同行排列 | y6、高38，背景高50 | 上下各6、整排左右各8；所有底边44 |

字体、图标沿用系统字体与既有图标库，未嵌入PDF字体；参考更细小的触控面积没有直接等比套用。以上差异已明确，不宣称逐像素一致。

## 用户指出的问题及修复证据

- 背景曾处于顶栏局部堆叠层，遮盖旁边图层/天气；改为主页根背景伪元素，图层/天气保持前景，实际点击入口可达。
- 天气旧min-height44覆盖height38，底边50而其余44；统一实际/min/max高度38，border-box。
- 旧背景48时白框上6下4，不垂直居中；背景50后上下各6。天气底边44、3D顶边58，间隔14。
- 320宽图层文字曾换行；缩小该图标至18、间隔2，文字单行，白框仍38高。
- 矮横屏上下按钮组可能相碰；宽>=600且高<=600时右上三按钮横排、记录窗移至右下内侧。

## 响应式与交互结果

320×568、360×780、390×844、430×932、844×390、768×1024，主要主页矩形两两检查无交叠、文档无横向溢出。边界记录为同目录`layout-checks.json`；对应home截图已保存。视角窗248×164、内部模型110×118，在200px浮窗上限内。

记录收起/展开不改变“未开始”状态；视角窗口、更多入口和关闭正常。图层/天气入口实点可达。未申请新定位/方向权限，未把浏览器模拟当成真实GPS/后台录制验证。

新增旧布局选择器兼容：启动/导入时在内存中将已知主页选择器及锚点映射到新类名，保留数值、v1键和格式，原草稿不自动写回；显式新选择器优先，迁移幂等。新增两项回归，全套508项通过。复杂自定义布局对新内容的视觉位置仍需用户按自己的草稿检查。

final result: pass（本轮主页边界、居中、遮挡与响应式检查通过；视觉定稿待用户确认，不代表真机验收或逐像素一致）。

## 验证范围

类型、架构、508项测试通过；APK签名、版本、资源与发布状态见CURRENT_STATE。未验收Android真机触控/键盘/系统大字体/后台记录；未交付HarmonyOS原生包。后续先依据用户对当前主页的反馈修正，不自动重做内部功能页。
