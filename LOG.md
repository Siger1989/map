# 2026-09-08 追加静止闪烁调查与原生记录刷新优化
- 0.2.6已发布至GitHub Release，3个资产digest一致，规范APK链接HTTP206验证通过；代码2ff4550已与远端main核对。最终现场106秒内重绘/源事件计数不增长；记录在CURRENT_STATE，后续仍需用户提供实际闪烁场景。
- 用户追加仍闪烁、不关机。电脑现场600帧/14.359秒相同，无WebGL错误，未复现；只读工具增加有界渲染计数用于后续排查，完整限制见docs/map-flicker-investigation.md。
- 确认原生每1.5秒相同JSON被重解析，引发record/segments新引用和地图覆盖层重提交。新增成功快照缓存、重复失败去重，真实变化/错误恢复仍更新。3项新增、233项全量及类型PASS。
- 重建未发布的0.2.6 APK，签名及693资源验证PASS；75,901,898字节，SHA256 a6e9ee491b10ff3d514a3b0dc92b11b212c716585213107f4e6bc0b0eb917020。不宣称闪烁彻底修复，无真机/鸿蒙原生验收。

# 2026-09-08 选中直接续画与0.2.6 APK
- TrackPanel顶部继续优先当前selectedId，直接调用指定轨迹续画并定位末端；无选择才展开列表，当前草稿同样直接恢复。未改道路寻路、存档格式、GPS或照片模块。
- 类型、230项回归、网页/安卓网页及完整APK构建PASS。独立浏览器两条轨迹验证选中第一条只更新自身，未选择保留选择器；360/390布局PASS。详细日志、截图和限制见CURRENT_STATE。
- 本机只有原包名签名，0.2.5独立签名构建被约束拒绝；保留两套签名配置，最终交付原系列0.2.6/code13，与0.2.5独立版并存、不自动迁移数据。已告知用户并写入发行说明。
- APK 75,192,962字节，签名/CRC/689个资源核对PASS；无ADB设备，不宣称真机验收。鸿蒙版本未知，原生包未生成。

# 2026-09-08 新建优先、选择续画、自动存档与按钮可见性
- TrackPanel拆为新建主操作和继续绘制选择器，指定线路续画并定位末端；用户追加按钮不明显，加入浅底蓝边框/展开图标和蓝色继续所选按钮。
- useManualTracks新增startNew/complete，完成自动存档，新建/切换先保存有效草稿；旧finish仍仅停止绘制以兼容节点编辑。新建独立ID，续画保留创建时间/名称并更新updatedAt，失败不重置草稿。
- archive生成默认时间位置名称并基于最新存储写入；新增可选drawingLocation/updatedAt，解析时忽略无效新元数据而保留旧轨迹。起点地名复用现有粗坐标查询，离线用大概坐标。样本时间不伪造。
- 新增4项回归，230/230全量、类型、网页和安卓网页构建PASS。独立IPv6来源浏览器两条新建、自动保存、第一条续画仅更新自身、重载恢复PASS。截图/手机范围和清理结果见CURRENT_STATE。
- 新建/存档限制见docs/drawing-archive.md；本轮无新APK/鸿蒙包。

# 2026-09-08 画线入口放到底部主栏
- 用户要求功能显眼，已发可选澄清暂无回复；结合当前主界面按画线入口实施并告知假设。主栏五项行程/路线/画线/收藏/工具，画线独立选中与关闭焦点，旧工具入口保留。
- 手机图标上文字下44px按钮，360/390无横向溢出，天气与导航同排；浏览器入口/关闭焦点/路线互换PASS。类型、网页/安卓网页构建PASS；截图捕获限制见CURRENT_STATE。
- 无业务逻辑/存储改动，无安装包；更新说明并按授权同步。

# 2026-09-08 2公里比例尺仍可吸附道路
- 根因是roadSnap在zoom<12直接拒绝，而主道路zoom6即显示。移除硬门槛，按已渲染道路和屏幕距离判断，删除过期zoom提示并更新说明。
- 扩展适配器回归在旧代码成功复现失败，修复后11.49/10/8级匹配与完整弯道、远离道路不匹配、未加载/隐藏处理PASS；226/226全量、类型、双构建PASS。
- 用户同地点zoom11.49、2km比例尺浏览器实际连续画线PASS；三点全部撤销，无保存测试数据，截图与路口选线观察记录CURRENT_STATE。未生成安装包。

# 2026-09-08 画线只保留逐点模式
- useManualTracks固定points（含续画），道路与节点吸附默认开启；TrackPanel删除模式切换、平滑专用牵引杆设置和另起段入口，保留手动吸附开关、颜色、线宽。
- TypeScript、226/226测试、网页/安卓网页构建PASS；浏览器确认默认两项开启、直接逐点绘制、开关可手动操作。390/360 DOM无横向溢出，截图与捕获限制见CURRENT_STATE.md。
- 保存数据、原寻路和其他模块保持；未创建测试轨迹，测试页已关闭，未生成APK。更新网页与移动说明后按授权同步main。

# 2026-09-08 后续鸿蒙出包目标
- 按用户要求将后续APK发行同时考虑鸿蒙可用交付写入AGENTS.md/mobileREADME，明确版本待确认、当前仅Android工程、原生NEXT需适配签名与安装验证。
- 仅文档修改，diff检查PASS；本轮没有构建或发布安装包。用户机型/版本问题已发出，后续按回复确定目标。

# 2026-09-08 二级UI统一、允许道路断口跨越
- modern.css将共享浅色磨砂从主容器枚举扩展到glass浮窗，补齐画线/快速添加/导航/照片/图例及二级按钮样式，统一12px普通控件、14px圆角、蓝色选中、深红错误。收藏主操作保持蓝底白字，用户颜色和图片内容不改。
- 用户追加允许道路吸附强行跨越：DrawingSession在目标有道路而无路径时提交直线段，TrackDrawing用虚线预览，提示跨越后继续吸附；更新原「不提交断路直线」测试和说明，取消/撤销/恢复吸附PASS。
- TypeScript、226/226测试、网页/安卓网页构建PASS。浏览器跨越与继续绘制PASS；正常尺寸行程/照片/画线浮条、390/360收藏编辑与绘制按钮DOM PASS。无保存测试分组/轨迹，独立测试页已清理。
- 精确手机截图仍受捕获缩放问题限制，无真机/照片全屏流程验收；未生成APK。详情与截图索引见docs/secondary-ui.md、CURRENT_STATE.md。

# 2026-09-08 整段道路吸附与距离连接
- 原逐点只吸附端点，用直线保存/预览。新增roadPath道路折线最短路径，DrawingSession/TrackDrawing传递整段，draft原子提交/撤销，TrackLayer绘制时只显示选择节点。
- 用户指出稍远不能连接；真实道路MVT确认桥梁与普通道路被layer隔离。按用户后续「不细分，主要看距离」要求，统一使用距离判定、3米端点接缝，不再按layer阻断。
- 新增7项回归（含公开道路13/14级跨桥/跨图块fixture），226/226全量、TypeScript、网页/安卓网页构建PASS。浏览器13.9级较远两点整段沿曲线、单次整段撤销和二次清空PASS。
- 类型检查曾因撤销历史联合类型未缩窄失败，改成明确stroke操作后PASS。浏览器旧会话坐标操作不准确、locator不支持position选项；重置并读取正式API后原生截图坐标验证通过，不改应用绕开验证。
- 无新增依赖、未改已保存轨迹、照片/GPS/路线服务/存储格式，未生成APK。说明见docs/road-segment-snapping.md，截图/运行日志不提交。

# 2026-09-07 顶部地点搜索
- 新增PlaceSearch，复用现有Photon searchPlaces服务；500ms延迟、中文组合输入保护、AbortController取消旧请求，提供清空/关闭/短输入/无结果/网络异常提示及上下键选择。
- PlaceName提取当前中心地名hook供搜索占位复用，page将搜索放入原44px顶栏；选中暂停跟随并focusPoint。modern.css结果内部滚动，避开右侧图层按钮。
- PASS：TypeScript、219/219测试、网页与安卓网页构建；真实成都搜索与键盘跳转至30.65987/104.06331；清空/短输入/Esc/点图层收起；390/360 DOM无溢出。
- 手机截图有既有缩放捕获异常，精确视觉、软键盘、输入法和触控待真机验证；未生成APK。截图/日志与范围见docs/top-place-search.md。
# 2026-09-07 细图层列表
- modern.css 缩窄图层浮窗到248px，去掉图源入口大胶囊底色，12px文字/16px细描边图标/28×16px细开关；鼠标主输入32px行，触屏主输入44px目标。
- 首次验证发现此Windows浏览器同时报告鼠标与触屏能力，any-pointer排除规则令行高仍44px；改用主输入 pointer:fine + hover:hover 判断后，实测行高32px，双构建重跑通过。
- PASS：TypeScript、网页和安卓网页构建、598×628前后截图、390/360 DOM无溢出、图层开关/展开参数/图源返回/关闭。
- 精确手机截图因既有捕获缩放问题 BLOCKED；本轮未验收真机。Dribbble页面Human Verification，未操作验证；研究来源与范围见 docs/slim-layers-ui.md。
- 仅CSS和文档，无新依赖、图源/存储/渲染改动，未生成APK。运行记录 .openai/*slim-layers*.log；截图 artifacts/screenshots/layers-slim-*.png。
# 2026-09-07 图源页面返回入口
- 原因：图层进入地图图源时未保存父入口，仅有关闭；添加/预览的返回位于滚动内容中。
- 修复：page 保存图层/工具来源；ControlDock 复用44px标题栏显示返回；MapSourcesPanel 上报步骤标题和返回行为，删除接入标题栏后的重复内容按钮。读取时返回取消请求；保存提交中的预览返回暂禁用；返回修改保留表单，扫码退出沿用组件卸载停止相机逻辑。
- PASS：类型检查、219/219逻辑测试、网页和安卓网页构建；浏览器真实逐级返回、草稿保留、关闭、窄屏滚动后标题入口可达。
- 截图：artifacts/screenshots/sources-header-back.png 正常视口 PASS；390/360 DOM无溢出，返回52×44px，面板320/297px。两张窄屏截图仍有既有捕获缩放异常，精确手机截图 BLOCKED；真机与相机硬件未验收。
- 范围：无新增依赖，未改地图、图源服务或存储格式；未打包APK。详细输出在本地 .openai/*sources-back*.log。
# 2026-09-07 画线/剖面入口修正
- 剖面由右侧移至工具第一行，与画线并列。画线入口原藏在“路线→轨迹管理”，开始按钮又在长说明之后；本次显式命名并提前按钮。
- 从剖面开始画线时结束剖面交互、收起详情/预览，保留数据；浏览器两次地图点击生成9.5公里测试线段，已清除仅本轮草稿并恢复平滑模式；工具剖面恢复原对象通过。
- TypeScript、219测试、网页与安卓网页构建通过。390/360菜单全部入口在面板内，无横向溢出；正常截图 artifacts/screenshots/tools-draw-section.png。未生成APK，真机触控限制不变。

# 2026-09-07 玻璃质感、数值旋转与剖面布局修正
- 根据用户新反馈，改为真实 backdrop-filter 磨砂表面，独立圆形工具，压缩剖面横排字段和折叠/操作行；仍保留主要44px触控区域。
- 增加共享 X/Y/Z 数值编辑、拖动读数、归正及既有撤销。剖面归正为竖直朝北，保留位置/尺寸；浏览器测试后已撤销恢复。
- TypeScript、219全量测试、双入口构建通过；测试初次仅负零严格断言失败，使用几何角度容差修正。手机截图捕获异常仍保留在 QA 中，不能宣称真机或闪烁已验收。无新APK。

# 2026-09-07 A 方案紧凑 UI
- 浅色面板与蓝色状态共用到网页/安卓网页，路线切换并入标题，图层参数和说明渐进展示；保留真实地图、绿色相机和现有业务逻辑。详细变化见 docs/compact-thetrail-ui.md。
- 浏览器修正两处对比度回归：标记深色按钮、图层展开状态。TypeScript、217 现有测试、两入口构建通过；未发布 APK。
- 手机捕获异常：覆盖尺寸后 DOM 正确，截图内容约半尺寸。尝试原标签、独立标签和文档 viewport/reset API 均未取得可靠手机截图；停止重复猜测，按 browser-troubleshooting 文档保留正常视口证据。清理临时标签后 reset 成功，原标签确认 598×628。手机视觉验收保留 BLOCKED，详情见 design-qa.md，不据此改地图渲染逻辑。

# 2026-09-07 山兔更名与全球入口
- 用户将产品改名“山兔”，面向全球。移除固定成都/川西页头，首次打开和世界视角按钮不再回川西，允许缩小浏览世界地图。
- 网页/安卓显示名称、通知、分享和导出文件文案统一；保持原包名/签名/本机数据/备份兼容及真实地区数据署名，不修改历史Release。
- 文件与验证见 docs/shantu-global.md / CURRENT_STATE.md；无业务文件删除，原签名限制仍在。

# 2026-09-07 照片详情与分享
- 新增主动全屏照片查看、双指/拖动、标题备注/旋转/三色画线、信息JPEG预览及系统分享/保存；查看副本2560px，旧预览和重复导入保留编辑兼容。
- 海拔取EXIF/对应轨迹，天气按拍摄日期查询并保留来源、时次、缺失与失败状态；原子补录不覆盖编辑，不复活被删除照片。
- 文件、验证、原签名限制与回滚说明见 docs/photo-details.md 和 CURRENT_STATE.md；无新APK发布、无相册原图改写。

# 2026-09-07 记录精度接受门槛
- 核对系统定位、存点与跟随刷新：原固定80米门槛；本轮新增默认20米、5–80米整数设置，网页和安卓分别持久化，运行中对新点生效。记录格式和旧样本不改。
- 过滤时明确提示估计误差与设置值；不伪造精度、不补点、不自动贴路，也不更改定位请求频率。旧桥缺配置能力时保留80米显示并禁用操作。
- 模块、验证和待签名状态见 docs/recording-accuracy.md 与 CURRENT_STATE.md。

# 2026-09-07 实走存档与文件夹照片修复
- 正常实走保存包含时间，但统一手绘标题误导；续画保存会丢失 samples，反向/合并也可能打乱样本对应。新增来源显示与原始节点保护，改线另存手绘副本，照片选择兼容旧时间数据并回退失效选择。
- 保存后读回确认再清理检查点、进入照片页。新增系统文件夹选择，受控递归目录/类型/200张限额及取消处理，普通多选保留。
- 原发布证书与当前机器不同，已核验并固定公开包名/指纹；构建拒绝新建或错用签名，支持原密钥路径和不可安装的 UnsignedOnly 验证。没有私钥上传或新 Release。
- 逻辑回归、浏览器与构建最终结果见 CURRENT_STATE.md；没有真机目录选择验收。

# 2026-09-07 行程照片时间匹配
- 用户希望照片拍摄时间与已有轨迹坐标自动对应。选择独立 photos 模块与 Android 用户主动多选文件，不扫描整个相册；只读 DateTimeOriginal/OffsetTimeOriginal，保留无时区与缺时间提示。
- 同一轨迹分段、前后点不超过两分钟、速度合理时才估算位置；不跨暂停、缺失时间或重叠歧义，也不使用文件修改时间。时差支持整体分钟校正与逐张修改。
- 本机 IndexedDB 保存 960px JPEG 预览与关联位置；200 张/40MB，文件哈希+轨迹 ID 去重，事务内配额检查。地图44px触摸区小图，60px网格聚合；预览限高38dvh/320px。普通存档备份暂不含照片，应用内明确说明。
- 依赖仅新增 exifr 7.1.3，无其依赖项/新审计项；保留其他 lockfile 项，未执行批量升级。首轮 Node 测试命中 exifr CommonJS 导出差异，改测试导入；安卓返回 mock 原泛化匹配误认照片面板，改为按选择器模拟并增加照片返回用例。
- 初版导入面板设置项挤占预览，按真实手机截图拆为选择与确认两步、校时折叠、确认操作固定于面板底部。最终验证见 CURRENT_STATE。

# 2026-09-07 实走记录相机跟随
- 用户实走后发现地图不会随记录移动。旧 usePosition 的定位回调仅执行一次；useRecording 独立存点，没有接地图相机，也没有同步记录点到位置图层。
- 新增独立跟随状态，记录阶段直接读取已接受记录点（安卓原生快照/浏览器记录共用），避免另开定位 watch；普通定位使用现有 usePosition。开始/继续记录自动跟随，拖图/全程浏览/行程预览暂停，明确按钮恢复；相机只平移，不重置缩放、俯仰、朝向。
- 跟随过滤过旧和低精度数据、等待地图加载、跳过重复快照；后台不动画，回前台用最新点。暂停/结束记录关闭跟随，编辑操作期间禁用；恢复跟随清除行程预览。记录格式、服务和原 3D 控制器保持原样。
- 手机方向传感器的高频朝向更新会中断居中缓动；方向跟随模式下位置直接居中，普通模式保留短缓动，追加双模式同时跟随与缩放保持测试。
- 发布助手复制版本时误把 GitHub API 日期 2022-11-28 一起替换，首次查询 HTTP 400；修正固定 API 日期后上传、发布与附件哈希核验通过。问题仅在本机辅助脚本，不在应用代码。
- 首轮浏览器脚本首点恰好等于地图中心，过早继续导致第二点被正确判定为瞬移；改为等待首次跟随调用完成。第二轮严格坐标相等被浮点误差触发，改用容差；非产品故障。最终结果见 CURRENT_STATE。

# 2026-09-07 同步并完善最新说明
- 用户要求同步 GitHub 并加入最新详细说明。源码在本轮开始时已与 origin/main 一致，采用仅文档更新。
- 将 0.2.2 操作、离线覆盖、备份、图源边界、安装校验及验证范围汇总到独立说明，并修正 README/安卓/跨设备文档中的旧版本、旧菜单、剖面开启及无原生桥等描述。
- GitHub Release 只更新正文，保留构建提交、标签、APK 和既有附件；不为文档更新重打包或改版本。

# 2026-09-07 路线地点与行程条
- 用户截图表明旧起终点栏无法原位输入，且缺少途经点/拖动排序；按直接编辑有序地点列表改造。兼容旧 start/end 调用，地图选点新增数字下标，修复 index 0 被 truthy 判定漏掉的问题。
- 查询在输入停顿后执行，组合输入不请求，旧请求被取消；搜索与路线按服务分别节流。排序使用 Pointer Capture，支持触摸和边缘滚动，放手提交顺序。
- 预览用路线累计距离索引与二分查找定位，RAF 合并触摸更新；独立游标不替换 GPS，预览地图跳转跳过普通 moveend 请求副作用。色带渐变不伪造气象精度。
- 手机 390/360 触摸排序、地图起点选择、预览与收藏恢复通过；真实 Valhalla 三点验证通过。Android IME inset 适配加入本轮，待真机。

# 2026-09-07 保留原 3D 控制器
- 用户希望继续使用之前的 3D 控制器，因此恢复 CameraGizmo 默认显示，保持其原有模型/圆环交互，不重做控制器。随 APK 升至 0.2.1-test/code8。

# 2026-09-07 户外版优化
- 根据用户免 Key 开源图源回复，默认使用 OpenFreeMap / OSM；明确公开服务不等于国内托管。保留带非商业许可的既有地形与可选影像，不使用私人 Key 或 OSM 官方批量下载。
- 独立 outdoor 模块处理 GPS 记录、点线文件交换、存档合并与显式离线包；安卓服务与 WebView 分离，系统文件选择器访问用户选择的文档。
- 验证发现 MapLibre 自定义协议 JSON 请求必须返回解析对象，不能统一返回 ArrayBuffer。已按 request.type 分支修复，道路地名恢复正常。
- 第一轮 APK 因机器 JDK 默认路径过时而失败，改用参数指定现存 SDK/JDK；第二轮 Java/DEX 成功但 PowerShell 默认编码误读中文 AndroidManifest，改为显式 UTF8；第三轮 APK 构建成功。
- 浏览器定位短暂丢失原先直接暂停，改为保留 watch 等待恢复；永久拒绝权限才暂停。手机面板避开快捷按钮，地图来源初始收起仍可点开。
- 新增 fflate 0.8.3 支持有大小上限的 KMZ 解压；保留其余依赖版本，未执行强制升级。

# 开发决定

## 2026-09-06 剖面滑条跳动

- 用户反馈鼠标拖动海拔不稳定。实际同视角单向拖动记录到 min/max 在区域范围 -150/6000 与加载默认范围 -500/9000 间切换，导致 5 次逆向数值回跳。
- 修复限定在 SectionPanel：保留最近完整统计的范围，忽略加载时的临时边界；指针按住时锁定刻度映射，通过捕获、松手、取消和失焦结束交互。保留原三维地形实时更新节流，不扩大计算量。
- 补真实鼠标往返拖动、停住和移出控件松手回归，跨多轮异步地形更新仍保持范围和数值稳定。浏览器、107/107 测试、类型检查及双入口构建 PASS，详见 CURRENT_STATE。

## 2026-09-06 剖面实际渲染故障

- 用户要求直接查看画面；用本地真实 Edge 和相同视角复现旧纹理块状残留、切面不显示。使用浏览器 WebGL 调用诊断定位到 clear/drawElements 的 INVALID_FRAMEBUFFER_OPERATION（1286）和 FRAMEBUFFER_INCOMPLETE_DIMENSIONS（36057）。
- 根因：上一轮为减少瓦片数将剖面源 tileSize 改为 512，原地形为 256。MapLibre 6.7 共享 RTT 由 1024 改为 2048 后附件不兼容，导致后续绘制失败。最小修复是保持原尺寸，继续使用 LOD 控制远景负担，不改引擎内部代码。
- 填色直接读取同一裁切 DEM；GeoJSON 轮廓没有栅格源自带的粗细层级遮罩，新增覆盖裁切适配，去掉被细瓦片覆盖的粗轮廓，防止斜线横穿纯色面。
- 广角复核发现 DEM 边缘线性插值带来透明细缝，填色改用 nearest 后消除；浏览器回归新增原缝隙区域纯色像素断言。isSourceLoaded 在 setTiles 前可能短暂为真，验收同时等待剖面报告完成与轮廓来源就绪，避免将加载过程当作最终效果。
- 最终真实浏览器六场景（白色/改色/海拔/旋转/重复切换/窄屏）及 WebGL 错误检查 PASS；107/107 逻辑测试、类型检查、网页/Android 静态构建 PASS。新增真实浏览器回归脚本，避免再用数据测试代替视觉验收。具体截图、构建和同步结果见 CURRENT_STATE。

## 2026-09-05 三维海拔剖面及性能修正

- 用户明确剖面应保留 3D 相机；临时俯视 Canvas 实现未交付，改为原生可见瓦片 DEM 取 min(原高程, 切面海拔)，用独立来源恢复原始数据。模型用同一真实海拔裁切及封口。
- 最终外观要求：UI 原配色，只有切面纯色，可自定义，默认白色；边界为单根实线。取消浅色控制面板和双层描边。
- 查读当前 MapLibre 6.7 color_relief_style_layer.ts：虽样式验证可接受 Step，但 GPU 色带构建仅识别 Interpolate。已用狭窄边界过渡实现恒定纯色，不宣称仅通过样式验证即完成视觉验收。
- 用户反馈明显卡顿。查出 moveend 再 setTiles 导致已有原生视野数据全重载；移除这条重载路径，移动仅合并更新轮廓数据。采用更低远景采样密度/LOD 数量限制、350ms 合并海拔调整、移动中延后更新、连接轮廓短线与统计缓存。颜色变更不读取或重建地形。
- 优化后完整 105/105 测试与 TypeScript PASS；构建和 GitHub 同步结果随后记录 CURRENT_STATE。没有浏览器实际帧率测量，性能结论仅限去掉已验证的重复工作。

## 2026-09-05 模型本体拖动

- 复用模型射线拾取，让模型本体与名称标签进入同一长按移动流程；重叠时模型本体优先于底下路线节点。
- 拖动传递模型原地面锚点，不使用射线命中的顶面/地下表面坐标，保留手指偏移及尺寸、旋转、埋深。模型与名称同步预览，工具条显示实时经纬度，松手调用已有位置保存/高程刷新接口，取消及撤销继续有效。
- 新增长方体/圆柱/球体 × 地表/地下 6 组真实 Three.js 几何测试，覆盖拾取、模型和名称同步、参数保留、预览取消。坐标还原使用浮点容差，完整 91/91 测试与类型检查 PASS。

## 2026-09-05 本地字体授权下载
- raw.githubusercontent.com 的 Noto LICENSE 初次连接重置，随后两次有界请求超时。停止重复请求同一下载通道。
- 最小问题：官方 Noto 仓库的公开 LICENSE 文本下载失败，与字体本身或应用构建无关。
- 改用已提供的 GitHub 文件读取接口读取同一官方仓库 notofonts/noto-fonts 的 LICENSE，成功取得全文及 blob SHA c82d72e422e2d08c5ab439b6bac7c2177ea0c565，并随本地字形分发。

## 2026-09-05 国内图源与道路吸附
- GeoQ 官网的免费套餐说明针对其平台，无法单凭该页面确认旧公开切片接口的外部应用授权；map.geoq.cn 本机连接超时，未将其设为默认图源。
- 天地图提供公益性官方地图服务，采用需要应用 Key 的 WMTS 接入；本轮只准备自有 Key 配置与适配，不复用示例 Key。未取得本应用有效 Key，真实影像与关闭 VPN 后的可用性仍待验证。
- 栅格底图图片不含可供手绘吸附的道路矢量几何，因此道路吸附独立读取已加载的 OpenFreeMap/OSM 道路。国内模式只在用户开启道路吸附后加载此数据，不宣称天气、导航、区域外高程已完成国内化。

## 2026-09-05 路线选择与节点编辑

- 原因：手绘图层只绘制线/点，没有路线业务 ID 与点击拾取，也没有节点编辑事件；标记名称能编辑，模型实体未接拾取。
- 决策：添加独立 FeatureDragBridge，在正常浏览时观察按压，480ms 静止后锁定相机；临时预览与持久化分离，松手才写入，双指/Esc/系统取消均还原。保留既有绘制及双指控图桥接。
- 路线选择联动高亮/详情；已保存路线原 ID 内更新，草稿加入原撤销历史。共用接点同步移动，续画基线携带节点数据，避免旧位置残留。
- 模型实体射线拾取；长按名称移动，保留 DOM 与 GPU 几何避免打断触摸及拖动重建。标记位置支持撤销。
- 最终验证：TypeScript、完整 85/85 测试、网页和 Android 静态构建、差异检查、本地预览 HTTP 200 均 PASS。新增 17 项专项含模型拾取与手势取消，未做浏览器/实机触控验收；同步结果见 CURRENT_STATE.md。

## 2026-09-06 自由矩形剖面
- 用户要求把固定海拔剖切改为可自由摆放的矩形面，切掉朝向观察者的一侧。采用有限矩形沿法线延伸的裁切体，GPU 直接 discard 原始地形片元；保留原 elevation 源及原始地图拾取深度，免去调整时 setTiles/DEM 编码和上传。
- MapLibre 没有公开的任意地形裁切面样式，查阅当前 6.7 的地形 shader、custom layer 和 Program 源码后，将适配集中在本地图 WebGL 上下文的 terrainClip.ts；shader 名称/结构通过版本兼容测试保护，不修改 node_modules/全局原型。自定义图层提供局部坐标矩阵和地形交面采样；模型使用五个相交裁切平面和实际三角形封口。
- 64×32 面网格/90ms 合并更新控制封口成本，缺失采样不补零。保留有限面外山体，不生成开口侧墙；不是完整实体网格布尔。尚无手机帧率实测，不声称性能倍数。
- 浏览器初轮功能与常用手机竖屏通过；额外短屏回归发现退出后右侧入口分别被视角盘及展开署名覆盖。根据实际命中元素，短屏将视角盘移左、提高右侧工具层级，保留署名。最终结果见 CURRENT_STATE。

## 2026-09-06 暂停剖面功能
- 用户明确要求“先不要这个功能”。通过 config/features.ts 统一停用剖面，隐藏入口、强制当前剖面状态关闭，并阻止创建 TerrainClip，因此普通地图不再安装 shader 包装或切面图层。实现文件保留，方便后续恢复；没有恢复旧海拔剖面入口。
- 只改功能开关及接入位置，天气、导航、标记/模型存档不受影响。当前 APK 不重新生成，已安装版本不会随源码停用而自动改变。
