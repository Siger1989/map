# 0.2.36 已确认界面验收

依据：本轮用户对24图及此前窗口的最终文字修正；后续文字优先于旧图。设计图的说明、状态编号、红绿确认圈和示例地形不进入产品。

## 实际结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 主界面画线比例 | PASS：六项工具292×44，小色板164×80；390/360单行，266两行；全部地图操作目标44px，中心可命中，无页面横向溢出 | drawing-restored-final-390.png、drawing-palette-360.png、drawing-palette-266.png |
| 独立定位及侧栏视角 | PASS：编辑时侧栏、缩放、路线显示及独立定位可达；定位暂不自动跟随打断编辑 | drawing-active-390.png |
| 记录窗口 | PASS：收起、子设置返回、待保存恢复后保存一份行程、记录清理后回开始入口 | record-window-390.png |
| 行程完整信息 | PASS：轨迹图/数据/沿途、真实速度时间、彩色海拔图和旧分段资料 | trip-details-390.png |
| 海拔显示合并 | PASS：一项海拔图含统计/剖面，曲线颜色对应海拔；无速度的路线不显示速度选项；关设置保留图 | elevation-combined-390.png、elevation-retained-final-390.png |
| 模型/钻井 | PASS：钻井在添加模型下，口径/深度参数，固定圆圈箭头，保留其他模型精确调整 | drill-parameters-360.png |
| 旧照片升级 | PASS：旧表保留，元数据索引不含Blob，可见缩略图和单张原图按需读；真实JPEG EXIF正确 | photo-db-upgrade.png、photo-exif-real-file.png |
| 隐藏/返回 | PASS：隐藏目录仍保留原对象归属；打开不自动恢复；返回收藏保留目录展开 | 实际DOM操作及独立事务回归 |

文件位于 `artifacts/screenshots/ui-confirmed/`，Release图示ZIP包含全部所列截图和离线索引。均为实际App/组件与构造数据，非真机截图，也不是用户原始照片或路线。

## 已发现并修复

- P0：路线显示设置的祖先z-index低于路线卡，视觉面板中点击实际落到下方编辑。设置改用Portal置于页面层，按真实面板登记返回；最终实测图上信息可选，编辑不会被误打开。
- P1：打开路线卡后旧CSS隐藏底部导航、编辑中隐藏视角侧栏。删除这两处冲突规则，仍保留各自层级和手势桥。
- P1：浏览记录子设置时返回跳过子窗口；现在先关闭子设置。绘制色板同样先收起，再显示退出选择。
- P1：画线工具边框占宽导致保存换行；修正盒模型和触控宽度，360/390六项单行，266自然两行。
- P1：分叉入口暗中保存旧草稿；现在先处理现有草稿。无效恢复内容留在原键且不允许覆盖。

## 未验收项目

Android真实安装/系统相册部分授权/相机/后台记录/手指与双指触控仍待设备验收。浏览器部分地形和路网服务502不可用；截图中的卫星底图可见不等于所有在线资源验收。HarmonyOS原生包未生成。没有把这些标成PASS。
