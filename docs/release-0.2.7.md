# 山兔 0.2.7 测试版（2026-09-08）

[直接下载 APK](https://github.com/Siger1989/map/releases/download/v0.2.7-test/Shantu-0.2.7-test.apk) · [下载页与校验文件](https://github.com/Siger1989/map/releases/tag/v0.2.7-test)

## 本次修复

- 手绘轨迹列表增加「导航」，选中后可直接「导航所选轨迹」。沿保存的线形导航，默认步行，不重新规划成另一条道路。
- 收藏列表中的规划路线和轨迹均可直接导航。原规划保留出行方式、途经点和转向步骤；切换导航使用本次所选路线。
- 导航入口增加边框并保留44像素点击范围，保持紧凑布局。

手绘线没有路口转向数据时提供沿线指引、进度和剩余距离；不虚构转向。至少20米且线段连续才可启动；断开的多段需先连接。需系统定位权限及可靠定位，定位超时不会累计虚假里程。详情见仓库 `docs/saved-route-navigation.md`。

保留0.2.6的续画、自动存档和原生记录刷新优化。地图闪烁仍待具体环境复现，本版不宣称彻底解决。

## 安装与各平台状态

versionName `0.2.7-test`，versionCode `14`，包名 `com.guanyun.weather.preview`，安装名「山兔」，沿用原观云测试签名，可覆盖同签名的原系列及0.2.6并保留该应用数据。

**不能覆盖公司机器的0.2.5独立「山兔测试版」**：该版本包名和签名不同。本包与它并存，数据不自动迁移；请保留原应用中的轨迹。两套签名配置保持不变。

| 平台 | 安装产物 | 验证状态 |
| --- | --- | --- |
| Android 8及以上 | `Shantu-0.2.7-test.apk`，下载后由系统安装器安装 | 构建、签名、资源检查通过；无真机安装/道路导航验收 |
| 支持Android应用的HarmonyOS | 可尝试同一APK | 需按实际手机系统确认，尚无设备验证 |
| HarmonyOS NEXT / 原生鸿蒙 | 尚未生成原生安装包 | 缺少目标系统确认、鸿蒙工程与已配置签名流程 |

## 验证与校验

- TypeScript、236项回归、网页及APK构建通过。
- Chrome安装包网页验证手绘/所选轨迹/收藏导航、结束、导航中1.0公里切换1.8公里，所选线形和距离正确。360/390宽度无横向溢出，新增按钮约44像素触控范围。
- 内置浏览器定位后曾出现点击未送到页面，Chrome对照正常；手机实测仍待完成。浏览器尺寸覆盖不等于真机验收。
- APK大小：78,135,358字节。
- SHA-256：`5998b13a9ad9af427fe25e975d39179f4a3bdc0685cfb48d92182dd9065a5a70`
- 签名证书SHA-256：`a3aa453c7fa05d8a5d54a11c648edbcc02297b064db50e44bcfea2b0b91cd29c`
- ZIP CRC、707项网页资源检查通过；470张重压缩PNG解码像素一致，内置473张地形瓦片。

仍为测试版，沿用现有图源许可与署名。
