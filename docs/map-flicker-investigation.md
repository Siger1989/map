# 静止地图闪烁：本轮证据与已修复问题（2026-09-08）

## 0.2.8追加：手机APK与OPPO X8 Ultra

用户确认发生于手机APK。已修复一个额外的确定问题：RouteLayer/TrackLayer/GuidanceLayer/PositionLayer原先每次sync均setData并moveLayer，即使数据完全相同，且独立同步会改变图层相对顺序。新增overlayData以图源实例和序列化GeoJSON去重，统一覆盖层顺序并保持位于道路之上；迟到的底图仍重新排好，之后相同更新不改层序。回归包含重复100次仅1次setData、真实变化/重建源正常更新、迟到道路层和路线选择/删除。

定位跟随只在超出小幅漂移范围或精度显著改善后移动相机，保留原始位置和轨迹样本。普通GPS跟随精度门槛保持80米；网络大致位置允许显示与查看误差范围，导航和实走门槛没有放宽。

最终电脑三维山区对照（28.21081N/103.11579E，zoom13.14/bearing52.3/pitch26，道路与标注开启）：elapsed 4365→113604ms，约109.239秒，render35、sourcedata114、sourcedataloading80、道路加载事件9、道路错误0均未增加；道路已加载并可见。截图`artifacts/screenshots/roads-028-final.png`，计数`.openai/roads-028-{start,end}.log`。无ADB真机，不能据此宣称X8 Ultra闪烁已经彻底解决。0.2.8包含代码修复供手机验证，原0.2.7没有这些修改。

## 追加线索：主要是道路闪烁

用户明确指出道路闪烁，且此前确认地图静止时也发生。排查转向矢量道路，尚未确认整线消失、边缘抖动还是颜色/粗细变化，也待确认手机APK或电脑网页。不能把之前的原生记录去重当作此次道路问题已经修复。

已检查：道路源由`addCartography`添加一次，没有道路定时动画或反复`setTiles/setUrl`；MapLibre内部对相同visibility值有比较后返回，未发现道路被同值设置反复重载的证据。公开问题[地形缩放闪烁](https://github.com/maplibre/maplibre-gl-js/issues/1077)只作为对照线索，不能证明本机版本和用户场景根因相同。

现有只读地图检查增加`rendering.roads`：OpenFreeMap矢量源是否存在/加载完成、加载事件计数、错误事件计数、三维地形状态、缩放级别，以及主路/小路/描边的存在、可见性与缩放范围。`requests`是`sourcedataloading`事件数量，并非真实HTTP请求数量；`eligibleAtZoom`也不保证当前画面一定含有该类道路。未增加帧循环、地图UI、网络请求或位置日志上传。

两项回归验证缺源、加载中、错误归属、隐藏、小路12级门槛、历史16项上限和监听清理；全量238项及类型/网页构建通过。此次仅增加诊断，**没有确认修复道路闪烁，没有重发APK**；0.2.7安装包不包含此轮新增诊断。

电脑对照位置28.21081N/103.11579E，zoom13.14、bearing52.3、pitch26。道路/标注开启，卫星/云雨/地质/等高线关闭。三维静止28.823秒内render=111、道路加载事件=30、错误=0保持不变，三条道路层均可见、数据已加载。关闭三维地形后，二维静止49.891秒内render=166、道路加载事件=30、错误=0同样不变。两个窗口没有道路重复刷新，但无法排除手机GPU/其他图层组合或采样窗口外的闪烁。截图保存`artifacts/screenshots/roads-static-3d.png`和`roads-static-2d.png`，仅检查道路存在与可见；计数保存在`.openai/roads-*.log`。独立测试页已关闭。

用户再次反馈地图仍闪烁。本轮没有复现用户描述的闪烁瞬间，**不能宣称闪烁已彻底解决**；需要补充出现于电脑网页还是手机APK，以及全图变白/变黑还是局部线条闪动。

## 已确认并修复：Android原生记录无变化也刷新

`useRecording`每1.5秒读取一次原生记录。原实现每次JSON解析都会产生新记录及新segments，即使返回字符串完全相同；上层据此重算轨迹与定位覆盖层并提交地图数据。静止、暂停、完成和未记录状态都可能发生无效工作。

新增`nativeRecordingSnapshot`缓存最近一次成功读取的原始字符串与解析结果，相同快照复用同一对象；仅有新定位点、记录状态或精度提示变化时更新。读取失败保留既有数据，重复失败不重复刷新，恢复时重新显示正常快照。原生轮询频率、定位精度门槛、权限、存储内容和记录点均不改变。

3项新增回归覆盖四种记录状态的重复轮询、新定位/命令/提示、损坏数据重试和恢复；全量233项通过。这是确定的刷新优化，**未证明它就是用户此次闪烁的唯一原因**。

## 电脑网页现场观察

- 当前现场为三维地形、道路和标注开启；云、雨、卫星、等高线、海拔着色和地质关闭，天气请求限流。没有采集到WebGL错误日志。
- 连续600张截图覆盖14.359秒，所有帧哈希相同。此结果仅覆盖该窗口，无法排除其他时间的瞬时闪动或宿主窗口合成问题。
- 现有只读`get_weather_view`增加有界渲染诊断：重绘、空闲、移动、尺寸变化、数据源事件和WebGL上下文丢失/恢复计数，最近最多16个事件。无逐帧像素读取或网络日志上传，不占用地图UI。
- 诊断记录显示本轮源码热更新会重新加载或同步覆盖层；它能解释开发时部分刷新，但不能拿来解释独立APK的全部问题。热更新重建地图时视角曾复位，已恢复用户原视角；保存的轨迹/标记未改动。
- 最终同一地图实例在82.249秒与188.054秒的两次读取中，render=36、sourcedata=160、sourcedataloading=84均未增加，也未记录上下文丢失/恢复；此106秒观察窗口中没有持续重绘链，不能据此推断其他时间/设备也无闪烁。
- 截图与诊断文件留在本机`artifacts/screenshots/`、`.openai/`，不上传包含用户路线的截图。

## 仍需验证

未连接ADB真机，不能确认手机安装、触控、后台记录或GPU表现。收到具体闪烁场景后，以同一地点、图层组合和可见症状继续对照，结合渲染/数据源/上下文计数定位。鸿蒙系统版本仍未知，原生鸿蒙包未生成。
