# 山兔0.2.40 — 主页PDF首轮对照

基于0.2.35回退基线，只修改主页这一层，供用户逐项对照；不是整套UI定稿。

- 顶部品牌/搜索/图层/天气；右上2D/3D、视角、更多；右下缩放、路线显示、独立定位；底部五入口。
- 视角复用原模型，更多包含框选对象、当前路线区域总览、定位设置；主页显示独立记录窗与行程点列表。
- 原轨迹、照片、标记编辑、地图引擎、包名、签名和存储格式保留。路线卡数据区仍保留旧内容，等待视觉反馈。
- Android 8+ APK，版本0.2.40-test/code47，可覆盖原独立测试版；未经真机安装/触控/GPS/后台验证。
- HarmonyOS6.1原生HAP/APP未交付。此APK不代表原生鸿蒙支持。
- 390×844与360×780主页截图、浮窗尺寸和相关浏览器交互已检查；合成示例路线仅在独立浏览器预览，不在包中。
- Windows构建脚本对短暂APK共享锁增加有上限重试，不忽略其他构建错误。

APK SHA256：`bbeebf8167d383a482686eb14d3d3c44851a37097855be9ad40b04203b93fa81`。
