# 山兔 0.2.24-test：紧凑剖面编辑、横版图纸与方向角

点位编辑移到地图下方的独立区域，点位操作区高112px，包含经纬度直接输入、资料、删点、调方向/沿线移动及已有标记重选；完整编辑带高164px，地图实际结束于编辑带上缘。顶部保留勘探线名称和图纸入口，底部保留加点、添加标记、完成编辑。

点击“调方向”或“沿线移动”即进入带提示的移动步骤，可拖动目标点，也可点击地图落位。A/B 转向保持 C/D 与沿线标记距 A 的里程；沿线移动保持方向，其他点始终约束在 AB 及延长线上。附加点确认删除后从剖面移除，关联地图标记保留；A/B 用于定方向，可改坐标或重选。

手机触屏追加处理：在SVG根元素预先声明手势限制，避免只设在SVG分组上的兼容问题；拖动收到取消事件时回滚预览并显示中断提示，不再静默结束。此项是兼容修正，尚未经用户手机验收，不能以浏览器鼠标拖动结果替代。

图纸继续采用原横版工程图排版，上为地形剖面，下为等高线平面图。标题下新增“方向角 A→B xx°（真北起顺时针）”，随 A/B 自动更新并印入导出图片。只调整查看界面的工具栏：顶部返回、信息、设置；底部分页、缩放、保存图片、分享。信息录入、完整资料附表及等高距设置保留。

修复 Android 保存平剖图时被文件名校验拦截的问题，改为 `Shantu-section-时间-页码.jpg`，主图和附表均可逐页导出 JPEG。点击保存后须在系统文件窗口选择文件夹，再点击系统“保存”；不自动进入相册，取消窗口不创建文件。照片及测量的原输出格式兼容。

## 修改范围

- 新增 `SurveyPointEditor.tsx`、`SurveySheet.tsx`，分别负责点位表单和图纸查看/导出；`surveyPointInput.ts` 严格校验十进制坐标，`exportName.ts` 统一导出命名。
- 修改 section 的 `SurveySectionPanel.tsx`、`survey.css`、`useSurveySection.ts`、`SurveyMapOverlay.tsx`、`surveyDrawing.ts`，完成独立编辑区、拖动/点击状态、金色选线及方向角标注。
- `app/page.tsx` 组合布局与回调；`modules/map/TerrainMap.tsx` 的取景接口增加可选边距，仅剖面传入专用边距。通过公开参数适配地图，不访问其内部状态。
- `NativeBridge.java` 增加严格 section 文件名校验，保留 JPEG 内容、体积、前台来源校验。Android 0.2.24-test / versionCode31。
- 新增导出与坐标/删除回归，补充横版与方向角更新回归；更新模块说明、设计QA和交接记录。

替换原点旁浮窗和堆叠工具布局，没有删除存档字段、旧矩形剖面或资料录入。测量计算/手势、路线颜色、收藏排序、相机和原Logo未修改；竖井、井深、岩层功能继续暂停。

## 验证

- 405/405 全量逻辑、类型检查通过，涵盖坐标边界、共线/里程保持、关联标记、导出名称、方向角90°转0°与横版尺寸。
- 浏览器真实拖动：B沿线拖动后坐标更新；拖动B转向后C的1021.6m里程不变；点击线外位置移动C会投影回线；取消、信息保存、删除D确认均生效。
- 390×844 / 360×780 无页面横向溢出；独立编辑带与地图边界相接，点位部分112px；360×480模拟键盘视口内坐标输入、错误提示和应用按钮可达。
- 图纸原1800×1480比例保持；35.48°方向角与20m等高距印入主图。信息/设置、放大/全图、分页及主图348303字节/附表145398字节的实际JPEG下载通过。独立验证页面console error为空。
- 最终网页和Android构建通过；v2/v3签名、zipalign、540项ZIP CRC、473地形资源、包内新UI/方向角/Android导出校验通过。APK 57673917字节，SHA256 `76d0624dbdecb3b1b27c3334923bb5999ef45d82b04c060a9f22b7a8be808b1e`。截图见 `artifacts/screenshots/ui-0224`，当前设计检查见 `design-qa.md`。

## 安装与平台状态

Android产物 `Shantu-0.2.24-test-standalone.apk`，包名 `com.guanyun.weather.shantu.preview`，Android8.0+，沿用独立测试系列4a94签名。可更新同包名/同签名的独立测试版；与原 `com.guanyun.weather.preview` 系列并存，不自动迁移数据，需要时用JSON备份导入。

本机ADB没有连接手机，未完成Android系统文件选择器落盘、真机手指拖动、输入法及分享验收。HarmonyOS6.1原生HAP/APP仍未交付；本APK不是原生鸿蒙包，不能声称支持原生安装。

同步分支 `codex/huawei-webview-touch`，未合入main；标签 `v0.2.24-test-standalone`。回退本轮UI提交可返回598afe4的保存修复版本，存档格式保持兼容。
