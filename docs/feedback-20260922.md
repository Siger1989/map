# 2026-09-22 累计反馈工作区交接

更新：用户随后明确要求打包，以上反馈代码已进入0.2.51-test/code58构建；55项定向测试、类型、安卓网页和Java/DEX/APK构建通过，签名/资源已核对。新包和交付记录见CURRENT_STATE及release-0.2.51.md。下文描述的是此前工作区阶段，列出的真机限制及后续审查点继续保留，不再把Java/APK编译列为未完成。

当前阶段：用户持续在右侧预览指出界面问题，按最小修改、定向验证、截图确认推进。尚未重新出包；0.2.50 Release不含下列工作区改动。不要自动把未确认的视觉改动发布为定稿。

## 已在本轮预览核对

- 390×857 OPPO Find X8比例不恢复桌面；顶部路线卡和行程点左右并列，底部信息可通过路线显示三个开关关闭。
- 路线显示设置暂隐顶部卡，避免堆叠；再次点击入口关闭。普通路线起终点、正反向行程/海拔与派生导航目标同步。
- 右侧工具栏高于保持原位的摇杆12px；导航左侧图例、锁定、记录不重叠。锁定导航保留里程进度条和实时数据；普通闲置锁定不渲染空白数据栏。
- 30项定向逻辑检查与类型检查通过；导航锁定/三显示开关/方向切换实际组件验证通过。390/360实际布局数据与截图见CURRENT_STATE。独立验证使用模拟高程与地图占位，不能当真机定位验收。

## 已实现、需继续验证后统一交付

- `tracks/recordedStats.ts`、`RecordedDetails.tsx`、`RecordedElevationChart.tsx`、`recordedProfileData.ts`：按原采样计算有效时长、速度、缺测及高程，海拔图按区间速度着色/滑块逐点查看。不要把没有时间的手绘路线显示为实走。`RecordedElevationChart.tsx`采用不同basename避免Windows/Vite与数据模块解析冲突。
- `routeEdit.ts`、`useManualTracks.ts`：仅改样式保留原ID/source/samples，真正改几何才生成“编辑副本”，不覆盖实走原件；固色清理旧边颜色。详情直接改整条路线色。
- `collections/WorkbenchPanel.tsx`/adapter/tree：地图对象增加显示/隐藏；规划收藏与测量增加独立可见状态并接地图图层。需复核全局tracks.visible与各对象开关、选中剖面状态是否一致，勿丢旧收藏数据。
- `controls/useMapFocusLock.ts`及TerrainMap相机接口：隐藏锁定，地图手势结束10秒回原视角，原跟随状态则回当前定位。需继续实际验证双指/连续拖动、返回键解锁与手势期间隐藏对象选择。
- `position/useScreenAwake.ts`、MainActivity、NativeBridge：原生常亮意图保留到恢复前台；浏览器WakeLock由单处管理。`CompassSensor.java`提供绝对旋转向量，定位按钮循环包含指南模式。尚未Java/APK编译或真机方向验证。
- RecordingService/Store与Manifest：前台录制CPU锁、START_STICKY恢复、断点保留；历史断录原因无法凭截图确定，系统强制停止不能保证自动恢复。需完成原生构建并在设备验证长时间记录/系统回收。
- `outdoor/offline.ts`/downloadPlan/OfflineDownload：14级内保留宽范围，15–16级沿线各1km，17–18级各250m，说明明确；仍有20k资源/1GB保护，长路线需调整。需复核容量超限按钮提示及缓存包详情对分级范围的展示。

## 后续交付

用户确认当前界面并要求统一交付时，完成上述必要验证、从最终源码提升版本构建、核对签名/内容、更新文档、提交推送当前分支并校验远端SHA、发布新测试Release。保持原签名/包名/存储键和原轨迹。不得上传附件、`.env.local`、`.openai`日志或密钥。原生HarmonyOS仍未交付，不能用APK冒充。
