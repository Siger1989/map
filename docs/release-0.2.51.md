# 山兔 0.2.51-test：路线信息、方向切换与导航锁定

- 选中路线卡与行程点在顶部并列，普通选中路线也可通过“路线显示”开关显示底部海拔曲线、全程数据和色标。设置窗打开时暂隐顶部卡；底部主菜单再次点击可关闭。右侧工具栏避开保持原位的摇杆。
- 选中路线显示起终点，行程点提供正反向切换，同步底部海拔图与派生导航行程，不改实走原件和时间序列。
- 实走详情增加有效采样时长、速度、移动估算与暂停/缺测等信息，海拔图按区间速度着色并可拖动逐点查看。整条路线颜色可保存；只改样式保留原实走档案，改几何则生成明确命名的编辑副本。收藏地图对象提供显示/隐藏按钮。
- 全局隐藏锁定在地图临时拖动结束10秒后恢复锁定视角。导航锁定保留左侧里程进度条、起终点、实时数据和解锁入口；普通浏览锁定去掉空白数据栏。导航图例、锁定、记录按钮按间距排放。
- 定位循环加入安卓原生指南传感器；常亮请求在恢复前台时重新应用。录制前台服务增加CPU锁与系统重建恢复，缺测区间保留断点，不补造轨迹。
- 沿线离线缓存高等级采用分级范围：14级内保留用户选定宽度，15–16级各1km，17–18级各250m；仍受资源数量和容量上限约束。沿用天地图7类WMTS与收藏离线包入口。

修改范围：controls、tracks/routeDisplay、collections/workbench、guidance/position、outdoor及安卓外壳接线。保持包名、原签名、存储键、照片目录、轨迹样本和用户布局兼容；没有迁移图片默认保存文件夹。未新增服务凭据或把本机配置提交仓库。

验证：类型检查、55项定向逻辑测试、安卓网页生产构建、Java/DEX/APK构建通过。安卓兼容CSS检查发现兄弟关系选择器后已改用等效显式类。签名v2/v3、zipalign、版本58、543项ZIP内容及473项地形瓦片检查通过；包内11项网页功能与3项原生功能标识通过。390×857与360×780的实际组件布局、信息开关、正反向海拔汇总及锁定进度条已验证；独立布局图使用占位地图，不能代表真机触控/定位验收。

安装：Android 8.0及以上，`Shantu-0.2.51-test-standalone.apk`；同签名山兔测试版可覆盖安装，无需先卸载。大小57,768,374字节，SHA256：`b897a04089c30bb542cb91429109601ed7036681442c39e81c6885ed1662d038`。

真机限制：指南方向、导航常亮、系统回收后的长时间录制和离线飞行模式仍待用户设备复测。不能仅凭截图判定历史断录原因；系统强制停止不保证自动恢复。原路线不同段点击消失的问题仍未明确复现。

平台交付：本次产物是Android APK。HarmonyOS 6.1原生HAP/APP仍未交付，现有工程和工具链/签名条件未完成，APK不代表原生鸿蒙支持。详情见 `docs/harmonyos-6.1-install.md`。

源码发布分支：`codex/rollback-ui-0235-20260921`，测试Release：`v0.2.51-test-standalone`；不声称合入main。
