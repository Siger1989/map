# 山兔 0.2.43 测试版：记录保存修复

- 修复记录显示已有点但保存禁用：暂停或长时间断点形成的单点分段现在可以保存，保留全部点、原始时间和分段，不把断开的点连接成路线。继续沿用实走轨迹来源和原存储键。
- 待保存状态显示“保存 / 继续记录 / 全程”；空记录保留重置入口。保存与GPX导出不再要求已有两点线段，保存持久化确认后才清除临时记录，失败保留原记录。
- 正常GPS定位成功不再弹精度横幅；定位等待、权限及方向错误仍保留。定位采样精度门槛没有改变。
- 定向记录/照片测试6项及类型检查通过，包括三个独立点保存回读、分段时间保留和持久化失败保护。APK重新生产构建，检查签名、版本、zipalign及必要资源。未重复全量测试；浏览器连接失败，未补截图和真机操作验收。

Android安装包：`Shantu-0.2.43-test-standalone.apk`，versionCode 50，Android 8.0及以上。下载后打开APK覆盖安装同系列山兔独立测试版；保留已有数据，不需要卸载旧版。请在原待保存记录上点“保存”，保存结果可在收藏查看。

HarmonyOS 6.1原生HAP/APP仍未交付，本APK不代表鸿蒙原生支持。
