# 2026-09-23 反馈处理进度（0.2.57本地预览）

最新路线编辑和标记图案修改见CURRENT_STATE顶部。用户现要求先出APK，已从当前源码全新构建0.2.57-test/code64本地包；源码提交、Release及远端校验状态以CURRENT_STATE顶部为准。

## 地图缩放卡住、局部高程色块

尚未复现用户真机故障，不能认定唯一根因。UI仍响应不代表地图瓦片、WebGL和地形正常。已修正RasterLevelLock在source元数据未就绪时临时移除层级限制的行为，并增加图源/瓦片/DEM/WebGL上下文与渲染诊断；日志不输出含Token的请求URL。不能把该代码风险直接写成用户卡死的确定原因。

图源增加每源WGS84/GCJ-02/BD-09栅格对齐，统一在WGS84地图上渲染；worker最多2并发、队列96、20秒超时、48块/16MiB校正缓存。XYZ/TMS/WMS模板可接入；标准WebMercator瓦片校正不等于支持百度专有瓦片矩阵，GeoTIFF image暂不支持。360窄屏图源主页一页可达、无内部纵向滚动；坐标选择持久化。合成worker像素误差小于1色阶，实际Sentinel校正瓦片加载通过。

还需真机同位置/图源/层级下持续缩放、拖动、旋转，确认画面和触控持续响应；截图局部地形着色问题尚无同场景复现。

## DEM审计

当前统一URL为/api/terrain/{z}/{x}/{y}.png?revision=repairs-v1，256像素Terrarium、maxZ12：局部成都FABDEM V1-2约30m补丁，其他地区Mapzen/SRTM回退。不能把局部增强说成全球30m。3D和地形着色共用URL但各有source；等高线、点查询、行程采样和剖面各有缓存/取样策略。

可进一步统一provider、版本、垂直基准、缓存和采样；路线原始GPS/文件高程须保留来源，与DEM估算区分。本轮只审计，没有下载/替换GLO30或完成DEM模块统一。现行局部数据的非商业许可不变。

## 导入与文件关联

路线和收藏都有共享导入入口，记录首页移除导入。选文件显示读取/错误/预览，确认后实际选择路线并定位地图，保留原有数据。增加Android VIEW/SEND文件读取桥，但微信真机验收需要后续新包。

已直接解码用户L013.ovobj：74点、约5.52公里，浏览器加载地图成功。新增OVOBJ已知v105结构、OVJSN、FIT、TCX、GeoJSON、CSV/TSV读取器，保留GPX/KML/KMZ/OVKML/OVKMZ/JSON。详见[格式范围、证据和限制](route-format-compatibility.md)。不能承诺所有私有版本通用。

当前分支codex/rollback-ui-0235-20260921；用户已明确交付，以上功能进入0.2.57-test/code64最终APK，源码与Release发布结果见CURRENT_STATE顶部。轨迹、照片、布局、存储键、签名保持兼容。

## 等高距、海拔文字及运动方向

追加框选问题已定位并修复：路线编辑工具条覆盖框选出口、拖拽矩形留存、模式退出丢选择、对象结果重新框选被panel副作用清空。共享框选输入层，连续加选/减选、松手清框、退出保留并恢复上级编辑，390/360实际操作通过；原对象未删除或改写。最终包已安排重新构建，详见发行说明。

用户明确30米指等高线高差间隔，已提供30/50/100/200米并记忆选择；低缩放自动稀疏。普通线也显示海拔，最新标注字号11px、沿线间距210px、描边1.4px并保留避让。当前DEM未换源；不提供之前误解的水平精度切换。

方向菜单新增运动方向朝上，以GPS航向或可靠位置位移计算，线性加速度辅助静止抑制；停车和低质量定位不乱转。Android传感器随模式/前后台管理。最终源码614项测试、TypeScript、新APK完整性检查通过；此前390/360布局及等高距重载记忆通过，最新手动终点尚无新浏览器截图。实际步行/骑行、微信文件打开、连续缩放仍待真机验收。详见[0.2.57发行说明](release-0.2.57.md)。
