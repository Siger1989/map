# 统一 UI 迁移与验证 · 2026-09-25

用户确认首页/路线样板后，按 [正式标准](ui-standard.md) 扩展所有面板的共享皮肤。功能入口、数据格式和地图内容颜色保持原语义。新用户默认深色黄绿；有效旧个性化不自动清除。此次是本地视觉迁移，不是APK/全业务验收。

## 实施覆盖

| 界面族 | 实施内容 | 本轮验证 |
| --- | --- | --- |
| 主页/路线/画线/工具 | 根主题常驻、共享卡片/输入/按钮、路线详情编辑与导航启动令牌映射 | 五主入口390/360；路线既有样板回归；空轨迹管理与工具入口 |
| 记录与设置 | 20px卡片、12px按钮、44px主操作、固定标题/内容滚动、16px输入；保留轨迹色样 | 390与390×480展开设置，实际输入16px、标题可见、竖向滚动 |
| 照片/离线 | 子页面控件、照片查看器外壳、离线范围操作卡、下载状态；保留照片及透明地图选区 | 照片导入入口实开；文件/相机/下载未执行 |
| 收藏 | 主列表、离线项、管理/导入/Excel、选择态、根节点弹窗与搜索联想；保留文件夹自定义色 | 390/360、打开与关闭新建文件夹、导入页、body portal可见；无新数据写入 |
| 图层/图源 | 图层卡与切换器、图源库/本机管理、数据来源说明 | 图层390/360；图源两级菜单、返回/关闭 |
| 天气/时间 | 卡片、数据分组、操作及文本 | 390实开；保留实际天气值和来源 |
| 标记/模型 | 入口、PinEditor各参数/选择/确认控件、对象调整、16px输入、危险/保存语义 | 标记/模型选择页实开；数据编辑状态按源码覆盖，未新增标记验证持久化 |
| 测量/剖面 | 操作面板、子选项、剖面列表/编辑/信息表；图表/工程纸保留原内容配色 | 测量与更多选项390；剖面列表390×480；未新建测量数据 |
| 帮助/个性化/布局 | 共享卡片、所有配色字段和布局工具皮肤 | 帮助与配色实开；默认值对应标准；旧保存/各预设对比度由测试覆盖 |
| 分享/导航详情等数据子窗口 | route-surface/route-dialog及编辑工作栏共用主题，保留QR/迷你地图/图表语义色 | 源码覆盖与构建通过；空收藏下未逐一触发有数据分支 |

## 证据

- TypeScript PASS：`.openai/ui-standard-types-20260925.log`。
- 定向测试 11/11 PASS：appearance、layout-home-migration、route-stops-scrub；`.openai/ui-standard-tests-20260925.log`。包含旧配色兼容与各预设按钮/活动态对比度回归。
- 移动网页构建 PASS：`.openai/ui-standard-build-20260925.log`，4.72秒；仅既有大chunk警告。
- `git diff --check` PASS。
- 截图统一位于 `artifacts/screenshots/ui-standard-*.png`，文件名包含页面和390/360/480。record-settings、photos、track、tools、sources、layers、favorites、folder-dialog、import、appearance、measure、measure-options、annotations、weather、time、survey均有截图。360主面板无横向溢出；记录480为200px卡片，固定36px标题；正文垂直滚动，焦点描边不产生横向滚动条。
- 修复验证过程中发现的记录强制3px圆角/主按钮色、收藏离线项白底、portal根背景盖住全图、图层/测量顶栏净空。最终保留默认390×约857实时预览。

## 验收边界

2026-09-25高亮补充：统一共享控件为2px内收焦点；路线活动分组弱底色，不叠加父子两层亮框；收藏无候选不显示空联想弹层；“收起搜索”关闭搜索区但保留输入；记录沿途两按钮改成等宽双列图标竖排。3个Luna子agent并行执行，主agent整合与浏览器审核。390/360收藏搜索、路线输入及记录按钮、360收藏新建弹窗、390×480记录焦点截图PASS，证据为 `ui-*-fixed-*.png`；TypeScript、16项定向测试、Android Web构建及diff check PASS。焦点规则源码覆盖其余主面板和子窗口，未将其记作逐一实测。

这是共享样式迁移与列明页面的浏览器验证。未覆盖真实数据下的每个加载/错误/删除/分享分支；未调用相机、开始GPS记录、下载离线区域、提交导入或写测试收藏；未进行Android真机IME、安全区、户外强光、触控验收。后续有真实数据时按标准继续核对这些状态，不能把本表写成全业务/真机验收。APK、提交与发布沿用快速视觉阶段规则，待明确交付。
