# 山兔 0.2.70 测试版（已退回草稿，后续由0.2.71取代）

GitHub 预发行版 `v0.2.70-test-standalone` 已退回草稿并保留原tag和两个资产，当前不是可公开下载的版本。该预发行版曾短暂公开；根代理核查时下载数为0，不能据此说已有用户通过GitHub下载或完成设备验收。

用户随后在设备上测试并报告框选双指地图手势方向反了、地图不能旋转且操作卡顿；另报告收藏夹导入入口和多层面板叠放问题。因此0.2.70被标记为已 superseded，缺陷修正和新的收藏导入/面板协调纳入0.2.71准备。保留本文件仅供追溯。

## 0.2.70本地构建记录

- 版本 `0.2.70-test`、versionCode77；包名 `com.guanyun.weather.shantu.preview`，沿用4a94测试签名。
- 本地APK `APK/Shantu-0.2.70-test-standalone.apk`，57,838,684字节，SHA-256 `960ba38b9e61953b3f0cac0a1fcf291263ab9df2b69321c45f07a4c4abd95621`。GitHub草稿保留此APK与同名 `.sha256` 文件；现不提供下载入口。
- 构建时 TypeScript、网页构建、Android独立包构建及根代理报告的640项逻辑测试通过；v2/v3签名、zipalign和473张地形瓦片通过。上述构建检查未覆盖用户后来报告的双指方向、地图旋转、响应延迟和面板堆叠行为。
- Android真机覆盖安装、数据保留及HarmonyOS 6.1原生HAP/APP均未验收/交付。
