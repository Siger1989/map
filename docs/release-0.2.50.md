# 山兔 0.2.50：收藏离线地图、路线快捷缓存与自由缩放

- 收藏默认列表增加“离线地图”文件夹，读取既有离线包索引，显示名称、图源、级别、大小和完成状态。可打开范围、继续/补齐、检查、确认移除；上版已下载内容无需重复下载，移除缓存不删除原路线。普通浏览地图产生的浏览器临时缓存不冒充完整离线包。
- 已选手画/导入路线卡片、规划路线摘要和已有路线的起终点页均直接显示“缓存当前路线”。复用当前路线全部分段，沿用两侧各5/10/20公里、默认10公里；下载窗“在收藏夹查看”进入同一离线文件夹。
- 旧锁级通过设置地图最小缩放限制缩小，这个相机限制已移除。控件明确改名“细节上限”：保留当前图源，放大时不超过所选影像级别，缩小使用同一图源概览瓦片。所有支持级别可直接选择，无须先放大。相机缩放范围保持原值。

边界：细节上限不是在全球所有缩放下固定同一批影像像素；供应方不同级别可能仍有拍摄批次/色差。它限制最大请求级别，放大后会使用已有像素放大，不承诺超出数据分辨率仍清晰。缓存仍在应用私有存储，不导出成相册文件、不迁移用户保存目录。

修改模块：`collections/OfflineMapFolder.tsx`/`offlineMaps.css`，CollectionsPanel/WorkbenchPanel接线；HomeRouteCard/RoutePanel/RouteResultSummary快捷入口；RasterLevelLock/RasterLevelControl及app。旧缓存键、几何、照片、收藏存档、用户UI布局、包名与签名保持兼容，天地图7类图源及下载算法不变。

验证：类型检查、13项离线/缩放逻辑测试、离线收藏DOM回归和网页构建通过。真实MapLibre覆盖45组缩放/俯仰/朝向组合，瓦片不超上限且请求数量受控；不修改调用方相机范围。既有缓存加载、打开、检查、移除确认/取消/删除、搜索通过。

UI：390×844及360×780独立Chrome真实组件/共享样式渲染。手画路线卡高148px、规划摘要162px、图层窗168px，下载入口完整可点，无横溢；收藏保留下方地图。截图 `artifacts/screenshots/cache-library-20260922/` 的地图和收藏内容是测试占位，不代表真机数据与操作验收。

安装：0.2.50-test / code57，Android 8.0及以上，`Shantu-0.2.50-test-standalone.apk`，同签名覆盖安装。APK原签名v2/v3、zipalign、版本、473项地形资源及新功能核对通过。SHA256：`9667ca23b04571d0050360fda14926b3b1d62fa18e6481b358bcc67c7b9768d0`。真机飞行模式/触控仍待复测；HarmonyOS 6.1原生HAP/APP未交付。
