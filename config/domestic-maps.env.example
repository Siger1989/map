# Copy this setting to the project root .env.local, then restart/rebuild.
# Use this application's own Tianditu browser application Key.
# This browser Key is public in the built website/APK, unlike server/private keys.
# Never paste someone else's demo Key or commit a real Key.
NEXT_PUBLIC_TIANDITU_KEY=
